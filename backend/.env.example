### Example .env for deploying to Shardeum
# RPC URL for Shardeum testnet
SHARDEUM_RPC=https://rpc-testnet.shardeum.org

# Your deployer private key (do NOT commit your real key)
PRIVATE_KEY=0xYOUR_PRIVATE_KEY_HERE

# Optional: other local settings
RPC_URL=http://127.0.0.1:8545
CONTRACT_ADDRESS=
INVESTMENT_CONTRACT_ADDRESS=
ADMIN_TOKEN=replace-me-with-a-secure-token

### INCO (confidential compute) configuration
INCO_BASE_URL=https://inco.example.local
INCO_API_KEY=replace-with-inco-api-key
INCO_PROGRAM=futureRentPayoutLogic_v1

### INCO Docs (optional)
# If you have an Inco Docs search service (SearchIncoDocs), set the URL and API key.
INCO_DOCS_URL=
INCO_DOCS_API_KEY=

### Oracle / Payout contract addresses
ORACLE_VERIFICATION_ADDRESS=0x...
PAYOUT_CONTRACT_ADDRESS=0x...

### Backend oracle signer (store real keys in vault not env)
# If using private key locally (for tests), set PRIVATE_KEY. In production use KMS/Vault.
ORACLE_WALLET_ADDRESS=0x...

### Monitoring / Alerts
# Configure your monitoring to detect repeated INCO compute failures. Example Prometheus labels:
# inco_compute_failures_total{service="payoutService"}
# Alert when rate > 5/min for 10m
